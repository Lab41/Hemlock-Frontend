<h3>Users</h3>
<div class="span8">
<table class="table table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Registered</th>
    </tr>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <%= user.inspect %>
    <tr>
      <td><%= link_to user.name, admin_hemlock_user_path(user.uuid) %></td>
      <td><%= user.email %></td>
      <td><%= user.created.to_date %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
